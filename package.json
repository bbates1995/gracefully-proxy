{
  "name": "gracefully-proxy",
  "version": "1.0.0",
  "description": "Proxy server to keep Anthropic API key off the device",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
